[tox]
envlist = py3
isolated_build = true

[pytest]

[testenv]
deps =
    ruff
    mypy
    pytest
    coverage
    psycopg2
    types-pyyaml
    pytest-postgresql
    pytest-cov
    bandit

commands =
    ruff format --check
    ruff check
    mypy src
    bandit -r src
    # pytest --doctest-modules --cov-branch --cov=src --cov-report=term-missing {posargs:src}
